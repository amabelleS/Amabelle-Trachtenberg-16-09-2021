import React from 'react';

import Text from '../Text';

import * as S from './style';

const FavoritesCard = ({ fav }) => {
  return <S.Card>FavoritesCard</S.Card>;
};

export default FavoritesCard;
